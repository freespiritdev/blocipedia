<h1><%= @page.title %></h1>

<div class="row">
  <div class="span8">
    <p><%= @page.body %></p>
  </div>
  <div class="span4">
    <% if can? :edit, @page %>
      <%= link_to "Edit", edit_wiki_page_path(@wiki,@page), class: 'button' %>
    <% end %>
  </div>
</div>